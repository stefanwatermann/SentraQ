# Beispiel-Konfiguration Sentraq Admin
# (c) 2026 Stefan Watermann, Watermann IT, Auetal (Deutschland), www.watermann-it.de
# ---------------------------------
# HINWEIS: Die tatsächlich verwendeten Konfigurationsdateien SentraqAdmin.config und
# SentraqAdmin.debug.config NIEMALS in das Source-Code Repository einchecken (siehe .gitignore).

# Name und Logo zur Anzeige in der Kopfzeile.
App.Header.Title = SentraQ - Administration
App.Header.Logo = applogo.png

# Zur Verschlüsselung des Session-Anmeldung verwendetes Salt.
# Applikations-eindeutiger String von mindestens 6 Zeichen länge (z.B. $Xu2%m).
Session.AuthenticationSalt = $Xu2%m

# Anzahl Tage, die der Session Cookie gültig ist.
Session.CookieDurationDays = 30

# Anzahl Tage, die er Authentication Cookie gültig ist. Default ist 1.
Auth.CookieDurationDays = 1

# URI und X-API-KEY für den Zugriff auf das Backend API
ControllerApi.Url = http://localhost:5183
ControllerApi.AuthKeyValue = FA0A4217-0AA5-4A26-985E-D38FD491D0FE

# Logging settings (DEBUG, INFO, ERROR)
Logging.LogLevel = INFO
